# Phase 4.2: Prenatal + Postpartum Programs - Context

**Gathered:** 2026-03-19 (updated 2026-03-19 ×2)
**Status:** Ready for planning

<domain>
## Phase Boundary

Build the complete API layer, service logic, repositories, and frontend UI for prenatal enrollment/visits/overdue/high-risk and postpartum delivery recording — on top of the ORM models and Pydantic schemas already delivered in Phase 4.1.

**Includes:**
- `PrenatalRepository`, `PostpartumRepository` (repositories/prenatal.py, repositories/postpartum.py)
- `PrenatalService`, `PostpartumService` (services/prenatal.py, services/postpartum.py)
- `routers/prenatal.py` + `routers/postpartum.py` (separate files, registered under main app)
- Phase 4.2 Alembic migration: adds delivery columns to `prenatal_enrollments` + `fp_method_chosen` to `postpartum_visits`
- Frontend Step 1: Prenatal tab + Postpartum tab on patient profile (individual patient view)
- Frontend Step 2: `/maternal/prenatal` and `/maternal/postpartum` as **Digital TCL pages** (wide ledger-style tables mirroring the DOH paper TCL format)
- Sidebar nav badges for overdue counts

**Build order (two steps):**
1. **Backend + Individual Profile tabs** — All repositories, services, routers, and the Prenatal/Postpartum tabs on the patient profile page. Nurses can enroll patients and record visits before the TCL pages exist.
2. **Digital TCL pages** — `/maternal/prenatal` and `/maternal/postpartum` as wide program-level list views. Built after backend is solid so the TCL can rely on a rich, JOIN-enabled enrollment+visits endpoint.

**Out of scope:** EPI (Phase 4.3), Nutrition (Phase 4.4), TB/NCD (Phase 5), FHSIS reporting (Phase 8).

</domain>

<decisions>
## Implementation Decisions

### API Route Structure
- **Patient-scoped operations** use nested routes under `/patients/{id}/`:
  ```
  POST /patients/{id}/prenatal/enrollment      ← enroll
  GET  /patients/{id}/prenatal/enrollment      ← get enrollment
  POST /patients/{id}/prenatal/visits          ← add visit
  GET  /patients/{id}/prenatal/visits          ← list visits
  POST /patients/{id}/prenatal/delivery        ← record delivery (closes prenatal, creates postpartum)

  POST /patients/{id}/postpartum/enrollment    ← standalone postpartum enrollment (no prior prenatal record)
  GET  /patients/{id}/postpartum/enrollment    ← get enrollment
  POST /patients/{id}/postpartum/visits        ← add visit
  GET  /patients/{id}/postpartum/visits        ← list visits
  ```
- **Program list operations** use flat routes that power the Digital TCL pages:
  ```
  GET /prenatal/enrollments                    ← active enrollments + visits by default (BHS-scoped) — primary TCL endpoint
  GET /prenatal/enrollments?status=active      ← explicit active filter (delivered_at IS NULL)
  GET /prenatal/enrollments?status=delivered   ← delivered enrollments
  GET /prenatal/enrollments?status=all         ← all enrollments
  GET /prenatal/enrollments?high_risk=true     ← filter: high-risk (combinable with status)
  GET /postpartum/enrollments                  ← active postpartum enrollments + visits (BHS-scoped)
  GET /postpartum/enrollments?status=completed ← all 3 visits recorded
  GET /postpartum/enrollments?status=all
  ```
  The primary TCL endpoint returns enrollments **with their visits embedded** (joined response) — the frontend does not need to make per-patient follow-up calls to render the wide table.
- **Router organization:** Separate `routers/prenatal.py` + `routers/postpartum.py` — keeps each program self-contained; consistent with EPI/Nutrition routers coming in 4.3/4.4
- **Role access:** nurse/midwife write, physician read (view enrollment + visits), CHO/DSO/PHIS coordinator read-only city-wide — mirrors Phase 3 consultation RBAC exactly

### Prenatal Enrollment Closure (Delivery Status)
- **Phase 4.2 migration** adds three columns to `prenatal_enrollments`:
  - `delivered_at TIMESTAMPTZ` — nullable; NULL = active enrollment, non-NULL = delivered
  - `delivery_outcome TEXT` — `'LIVE_BIRTH'` | `'STILLBIRTH'` | `'ABORTION'`
  - `delivery_place TEXT` — free text, nullable (BHC name, hospital, etc.)
- **Active enrollment query:** `WHERE delivered_at IS NULL AND deleted_at IS NULL`
- **Record Delivery endpoint:** `POST /patients/{id}/prenatal/delivery` — dedicated action endpoint (not a PATCH); atomically sets `delivered_at`/`delivery_outcome`/`delivery_place` and auto-creates the postpartum enrollment
- **Delivery response:** Returns a combined object with both the updated prenatal enrollment (now closed) and the newly created postpartum enrollment (with `day1_date`, `week1_date`, `week6_date`). Frontend updates both tabs without an extra API call.

### EDC Auto-Calculation
- **EDC is auto-computed from LMP — not manually entered.** Formula: `EDC = LMP + 280 days` (Naegele's Rule / DOH standard).
- **Schema change:** Remove `edc` from `PrenatalEnrollmentCreate` request body. Service computes `edc = lmp + timedelta(days=280)` at enrollment creation and stores it in `prenatal_enrollments.edc`. Nurse does not enter EDC; she enters LMP only.
- **EDC is always present** in `PrenatalEnrollmentResponse` and `PrenatalTCLResponse` — it's a core scheduling anchor for the midwife.

### AOG-Based next_visit_date Calculation (PRNT-03)
- **DOH-standard frequency:**
  - AOG < 28 weeks → `next_visit_date = visit_date + 4 weeks`
  - AOG 28–35 weeks → `next_visit_date = visit_date + 2 weeks`
  - AOG ≥ 36 weeks → `next_visit_date = visit_date + 1 week`
  - AOG is NULL on a visit record → default to 4 weeks
- **Capped at EDC:** If computed `next_visit_date > edc`, service sets `next_visit_date = edc`
- **Initial scheduling:** At enrollment creation, service computes current AOG from `lmp` (today's date − LMP in weeks), applies the same frequency rule, and sets `next_visit_date` immediately — never NULL after enrollment

### High-Risk Flag (PRNT-05, PRNT-06)
- **Auto-flag triggers — two evaluation points:**
  1. **At enrollment creation** (evaluated immediately, before any visits):
     - `para ≥ 5` (grand multipara — captured in enrollment form)
     - **Age < 18 or Age > 35** (DOH standard — service fetches `patient.birthdate` and computes age from LMP date or today)
  2. **After each visit save** (re-evaluated on every visit record):
     - BP ≥ 140/90 (hypertension in pregnancy)
     - Hgb < 11 g/dL (severe anemia)
     - GDM positive (checkbox on visit form)
     - Any enrollment-level trigger still applies (para, age)
- **is_high_risk never auto-resets** — once flagged (by auto-trigger or manual), only the nurse can unset it by editing the enrollment. Service auto-trigger can only SET to `true`, never reset to `false`.
- **Manual flag:** Nurse checkbox on enrollment. Reason stored in `risk_factors TEXT` (existing column) — no new column needed.
- **Service requirement:** `PrenatalService.enroll()` must fetch `patient.birthdate` (via a `PatientRepository.get()` call or passed in from the router) to evaluate the age condition at enrollment creation.

### Postpartum Visit Fields (PNPL-03)
- **Phase 4.2 migration** adds one column to `postpartum_visits`:
  - `fp_method_chosen TEXT` — optional; records FP method if nurse provides counseling and patient makes a choice
- **Pydantic validation** on existing TEXT columns (same pattern as EPI vaccine — validate at API layer, keep DB column as TEXT):
  - `wound_status: Literal['GOOD', 'SUBINVOLUTED', 'BOGGY'] | None`
  - `newborn_status: Literal['ALIVE_WELL', 'DECEASED', 'REFERRED'] | None`
- **Postpartum overdue detection:** `postpartum_visits.visit_type: Literal["day1", "week1", "week6"]` already exists. Overdue = no visit recorded with that type AND corresponding scheduled date (`day1_date`/`week1_date`/`week6_date`) < `CURRENT_DATE`

### Digital TCL Pages (Program-Level List Views)

**Concept:** `/maternal/prenatal` and `/maternal/postpartum` are Digital TCL (Target Client List) pages — wide, ledger-style tables that mirror the DOH paper TCL format. One row = one enrolled patient. Visit data spreads across columns. Nurses see the same clinical picture they would on paper, but live and filterable.

**TCL Status filter:**
- Default view: **Active only** (`delivered_at IS NULL`) — the most common working view, keeps payload clean (typically <100 rows for a single BHS)
- Status filter dropdown: `Active` (default) | `Delivered` | `All`
- **No pagination** — all rows for the selected status load at once; acceptable given BHS volume
- **Frontend name search bar** — local client-side filter on the loaded rows; no extra API call. Filters the visible rows by patient name as the nurse types.
- The `GET /prenatal/enrollments` and `GET /postpartum/enrollments` endpoints accept an optional `?status=active|delivered|all` query param (default: `active`)

**Prenatal TCL (`/maternal/prenatal`) columns:**
```
Name | LMP | EDC | G | P | V1 Date | V1 AOG | V1 BP | V1 Wt | ... | V9 Date | V9 AOG | V9 BP | V9 Wt | Latest/Notes
```
- **Column cap: 9 visit slots + 1 "Latest/Notes" column.** Visit columns V1–V9 show data for the first 9 chronological visits. If a patient has >9 visits, the 10th column shows the most recent visit data with a `+N` badge (e.g., "+3 more visits"). A DOH TCL has space for 4–8 visits; 9+latest covers even high-risk outliers.
- Overdue visit cells highlighted red using `--status-critical` token (the expected-visit-date cell is past due with no recorded date)
- HIGH RISK badge on the patient name cell (row-level indicator)
- Horizontal scroll expected; sticky Name column (first column, `position: sticky; left: 0`)

**Postpartum TCL (`/maternal/postpartum`) columns:**
```
Name | Delivery | Outcome | Day1 Date | Day1 BP | Wk1 Date | Wk1 BP | Wk6 Date | Wk6 BP | FP Method
```
- ~10–12 columns; fixed slot structure (no dynamic column expansion needed)
- Overdue slots highlighted red; completed slots shown normally
- Status filter same as prenatal: Active (default, delivered_at IS NULL) | Completed (all 3 visits recorded) | All

**Scoping:**
- Nurse sees own BHS patients only by default
- CHO/DSO/PHIS coordinator sees city-wide with a BHS filter dropdown (same `CROSS_BHS_ROLES` pattern from Phase 3)

**Badge refresh strategy:** React Query `invalidateQueries` on visit-record mutations — no polling. Badge re-fetches immediately after a nurse records a visit.

**Navigation from TCL row click:** Navigates to `/patients/{id}?tab=prenatal` (or `?tab=postpartum`). `PatientProfilePage` reads the `tab` query param to pre-select the active tab.

### Standalone Postpartum Enrollment (without prior prenatal record)
**Clinical reason:** Mothers who deliver at private hospitals or other cities return to their local BHS for postpartum care with no prenatal record in the system. They need a postpartum enrollment created directly.

**UI:** "Enroll in Postpartum Care" button on the Postpartum tab empty state (when no enrollment exists for the patient). Opens a Dialog/modal with fields:
- `delivery_date DATE` — required; manually entered by nurse
- `delivery_place TEXT` — optional; facility name or location
- `delivery_outcome TEXT` — `LIVE_BIRTH` | `STILLBIRTH` | `ABORTION`
- `prenatal_enrollment_id` — absent (NULL); this is the distinguishing field from the Record Delivery path

**Endpoint:** `POST /patients/{id}/postpartum/enrollment` — accepts the above fields directly (no prenatal closure step). This is a separate endpoint from `POST /patients/{id}/prenatal/delivery`.

**Service layer:** Both paths (`POST /prenatal/delivery` and `POST /postpartum/enrollment`) call the **same internal service method** — `PostpartumService._create_postpartum_enrollment(patient_id, delivery_date, delivery_outcome, delivery_place, prenatal_enrollment_id=None)`. The method computes `day1_date`, `week1_date`, `week6_date` from `delivery_date` and inserts the postpartum enrollment. The only difference: the `POST /prenatal/delivery` path also closes the prenatal enrollment first, then calls the shared method with a `prenatal_enrollment_id`.

### Claude's Discretion
- Exact SQL for AOG-based overdue detection query (join strategy between enrollments + visits)
- Postpartum overdue query joining enrollment + visits by visit_type
- Whether the `/prenatal/enrollments` flat list routes live in a separate router file (e.g., `routers/maternal.py`) or as additional endpoints in `routers/prenatal.py`
- Exact loading states and skeleton design for program tabs
- Error message wording for "patient already enrolled in active prenatal care"

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase 4.1 schema foundation (all 8 MCH ORM models already exist)
- `.planning/phases/04.1-mch-shared-data-model/04.1-CONTEXT.md` — All ORM models + schema decisions locked here; BHS isolation via enrollment JOIN; `delivered_at` pattern context; `record_status` enum; `lazy="raise"` rule
- `backend/app/models/prenatal.py` — PrenatalEnrollment + PrenatalVisit ORM (Phase 4.2 adds delivery columns via migration)
- `backend/app/models/postpartum.py` — PostpartumEnrollment + PostpartumVisit ORM (Phase 4.2 adds fp_method_chosen via migration)
- `backend/app/schemas/prenatal.py` — PrenatalEnrollmentCreate/Response, PrenatalVisitCreate/Response (Phase 4.2 updates to add delivery fields + Literal validation)
- `backend/app/schemas/postpartum.py` — PostpartumEnrollmentCreate/Response, PostpartumVisitCreate/Response (Phase 4.2 updates wound_status + newborn_status Literals + fp_method_chosen)

### Phase 4 program decisions (UI + overdue + high-risk layout locked here)
- `.planning/phases/04-maternal-child-health-programs/04-CONTEXT-PROGRAM-DECISIONS.md` — Program tab layouts, Prenatal tab structure, Postpartum tab structure, high-risk flag placement, Record Delivery dialog fields, newborn registration prompt. **⚠️ EXCEPTION: The "overdue list page" design in this file is superseded by the Digital TCL decision in THIS context file. Use the Digital TCL format (wide table with inline overdue indicators), not the two-section overdue+high-risk list described in the program decisions doc.**

### Phase 3 patterns (extend, don't replace)
- `.planning/phases/03-patient-itr-core-data-model/03-CONTEXT.md` — Route pattern (`/patients/{id}/consultations`), RBAC role set definitions, Sheet side panel for forms, tab structure on PatientProfilePage, `?tab=` query param navigation
- `backend/app/routers/patient.py` — existing router file; prenatal/postpartum routers registered alongside or nested here
- `backend/app/repositories/base.py` — `BaseRepository`, `CROSS_BHS_ROLES`; PrenatalRepository + PostpartumRepository must inherit from `BaseRepository`
- `backend/app/services/patient.py` — existing service reference pattern
- `frontend/src/pages/patients/PatientProfilePage.tsx` — existing Tabs component; Phase 4.2 adds Prenatal + Postpartum tab content here

### Requirements
- `.planning/milestones/v2-REQUIREMENTS.md` §Maternal Care — Prenatal (PRNT-01 to PRNT-06) — enrollment fields, visit fields, overdue detection, high-risk flag spec
- `.planning/milestones/v2-REQUIREMENTS.md` §Maternal Care — Postpartum (PNPL-01 to PNPL-04) — delivery link, schedule, visit fields, overdue detection
- `.planning/PROJECT.md` §Compliance Requirements — RA 10173 (soft deletes mandatory on all clinical tables), DOH DM 2024-0007 (FHSIS formulas aggregate this data in Phase 8)

### Existing migration reference
- `backend/alembic/versions/0005_mch_schema.py` — Phase 4.1 migration; Phase 4.2 migration must come after this (new file `0006_prenatal_delivery_columns.py` or similar)

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `BaseRepository` (`backend/app/repositories/base.py`): `PrenatalRepository` + `PostpartumRepository` inherit from this — `_isolation_filter()` handles BHS-scoping automatically for enrollment tables
- `backend/app/repositories/consultation.py`: Direct reference — pattern for a program-specific repository with BHS-scoped list queries
- `backend/app/services/patient.py`: Pattern for service layer that calls repositories and auto-sets `health_station_id` from `current_user`
- `frontend/src/pages/patients/PatientProfilePage.tsx`: Already has `Tabs` component with TabsList/TabsTrigger/TabsContent — Phase 4.2 adds Prenatal and Postpartum tab panels
- `frontend/src/features/patients/api.ts`: Existing API functions — Phase 4.2 adds prenatal/postpartum API functions to this feature or a new `features/prenatal/api.ts`
- shadcn `sheet`, `dialog`, `badge`, `skeleton`, `table`, `tabs`: All already installed

### Established Patterns
- **Pydantic TEXT validation:** `epi_vaccinations.vaccine` pattern — keep DB column as TEXT, validate allowed values with `Literal[...]` in Pydantic schema (PRNT-04.1). Apply same to `wound_status`, `newborn_status`
- **health_station_id auto-set:** Excluded from Create request bodies — service sets from `current_user.health_station_id` (established Phase 3, confirmed Phase 4.1)
- **recorded_by auto-set:** Excluded from Create request bodies — service sets from `current_user.id`
- **BHS isolation for visit queries:** JOIN through enrollment to reach `health_station_id` — never put `health_station_id` on visit tables
- **React Query invalidation:** After any mutation (create visit, record delivery), call `queryClient.invalidateQueries` with relevant query keys to refresh badge counts and lists
- **Tab URL state:** `?tab=prenatal` query param pattern — `PatientProfilePage` reads this to pre-select tab (to be implemented in Phase 4.2 alongside the new tabs)

### Integration Points
- `prenatal_enrollments.is_high_risk` → Phase 8 ML at-risk classifier input
- `prenatal_enrollments.next_visit_date` → Phase 8 FHSIS M1 maternal care indicators
- `postpartum_enrollments.day1_date/week1_date/week6_date` → Phase 8 FHSIS postpartum coverage indicators
- `delivered_at` (new column) → postpartum overdue detection starting point
- Newborn registration prompt → creates new `patients` record (same `POST /patients` endpoint from Phase 3) — no new endpoint needed

</code_context>

<specifics>
## Specific Ideas

- **Digital TCL concept:** The `/maternal/prenatal` and `/maternal/postpartum` pages are "Digital TCLs" — wide tables that mirror the DOH paper Target Client List ledger format. Nurses already know this format; the goal is to replicate it digitally with live data and visual overdue indicators.
- **Two-step build order** (explicit for planners): Build backend + profile tabs first, then TCL pages. This ensures all API endpoints are solid before the complex wide-table frontend is built on top of them.
- `POST /patients/{id}/prenatal/delivery` is an **action endpoint**, not a REST resource update — name reflects the clinical action rather than the data being patched
- Delivery response returns a combined shape: `{ prenatal: PrenatalEnrollmentResponse, postpartum: PostpartumEnrollmentResponse }` — frontend can update both tabs in a single mutation callback
- `next_visit_date` is computed at enrollment creation (not left NULL) so overdue detection has a baseline from day one
- High-risk is sticky — auto-flag can set it, only human unsets it. This prevents automatic clearing of a flag a nurse set for a reason not captured by the auto-check rules
- `wound_status` validation: `GOOD` / `SUBINVOLUTED` / `BOGGY` — these are standard BHS terminology for uterine involution assessment
- Postpartum overdue uses `visit_type` to match each scheduled slot exactly: a `day1` visit only covers the Day 1 slot, not the Week 1 slot
- TCL endpoint (`GET /prenatal/enrollments`) returns enrollments with visits embedded — the frontend renders the wide table from a single response without per-row follow-up calls
- **EDC is auto-computed, never entered manually:** `edc = lmp + timedelta(days=280)`. Remove `edc` from `PrenatalEnrollmentCreate`. Nurse enters only `lmp`, `gravida`, `para`, `risk_factors`.
- **Age high-risk check at enrollment:** Service fetches patient's birthdate and computes age. `age < 18 OR age > 35` → auto-sets `is_high_risk = True` at enrollment creation, before any visits.
- **TCL column cap:** 9 visit slots (V1–V9) + 1 "Latest/Notes" column. Patients with >9 visits show a `+N` badge in the last column. Prevents unbounded table width; DOH paper TCL rarely exceeds 8 visits.
- **TCL status filter default:** Active only (`delivered_at IS NULL`). Status dropdown: Active | Delivered | All. Frontend name search is client-side (no extra API call).
- **Standalone postpartum enrollment:** Same `_create_postpartum_enrollment()` service method used by both the "Record Delivery" path and the "Enroll in Postpartum Care" path. The only difference is whether a `prenatal_enrollment_id` is passed.

</specifics>

<deferred>
## Deferred Ideas

- FHSIS M1 maternal care report generation — Phase 8
- BHW offline entry for prenatal visits — Phase 9 (offline sync PWA)
- Email/SMS alert to nurse when prenatal visit becomes overdue — beyond capstone scope
- Prenatal referral tracking (referred_to facility, referral outcome) — not in PRNT-01 to PRNT-06 requirements
- Postpartum `fp_counseled` analytics (FP adoption rate per BHS) — Phase 8 FHSIS reporting
- Guardian/emergency contact fields on patient profile — deferred to Phase 8 demographics expansion

</deferred>

---

*Phase: 04.2-prenatal-postpartum*
*Context gathered: 2026-03-19*
