---
phase: 04.1-mch-shared-data-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/prenatal.py
  - backend/app/models/postpartum.py
  - backend/app/models/epi.py
  - backend/app/models/nutrition.py
  - backend/app/models/__init__.py
autonomous: true
requirements:
  - PRNT-01
  - PNPL-01
  - EPI-01
  - NUTR-01

must_haves:
  truths:
    - "All 8 MCH ORM models exist with TimestampMixin + SoftDeleteMixin"
    - "Enrollment tables carry patient_id FK, health_station_id FK, and recorded_by FK"
    - "Visit tables carry enrollment FK and recorded_by FK but NOT health_station_id"
    - "ML integration fields exist: is_high_risk, fic_status, severe_wasting"
    - "All relationships use lazy=raise"
    - "All 8 tables carry local_id UUID and status record_status columns"
  artifacts:
    - path: "backend/app/models/prenatal.py"
      provides: "PrenatalEnrollment and PrenatalVisit ORM models"
      contains: "class PrenatalEnrollment"
    - path: "backend/app/models/postpartum.py"
      provides: "PostpartumEnrollment and PostpartumVisit ORM models"
      contains: "class PostpartumEnrollment"
    - path: "backend/app/models/epi.py"
      provides: "EpiEnrollment and EpiVaccination ORM models"
      contains: "class EpiEnrollment"
    - path: "backend/app/models/nutrition.py"
      provides: "NutritionEnrollment and NutritionVisit ORM models"
      contains: "class NutritionEnrollment"
    - path: "backend/app/models/__init__.py"
      provides: "All 8 new models exported"
      contains: "PrenatalEnrollment"
  key_links:
    - from: "backend/app/models/prenatal.py"
      to: "backend/app/core/base.py"
      via: "import Base, TimestampMixin, SoftDeleteMixin"
      pattern: "from app.core.base import Base, TimestampMixin, SoftDeleteMixin"
    - from: "backend/app/models/postpartum.py"
      to: "backend/app/models/prenatal.py"
      via: "prenatal_enrollment_id FK"
      pattern: 'ForeignKey\\("prenatal_enrollments.id"\\)'
---

<objective>
Create all 8 MCH SQLAlchemy ORM model classes across 4 model files (prenatal, postpartum, EPI, nutrition), following the established Consultation model pattern from Phase 3.

Purpose: These models define the database schema contracts that Phase 4.2-4.4 repositories, services, and Pydantic schemas will consume. Getting the column names, types, and FK relationships right here prevents downstream rework.

Output: 4 new model files + updated __init__.py with all 8 models exported.
</objective>

<execution_context>
@D:/capstone-hsms/.claude/get-shit-done/workflows/execute-plan.md
@D:/capstone-hsms/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-mch-shared-data-model/04.1-CONTEXT.md

<interfaces>
<!-- Existing code contracts the executor must follow exactly -->

From backend/app/core/base.py:
```python
class Base(AsyncAttrs, DeclarativeBase):
    pass

class TimestampMixin:
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now(), nullable=False)
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now(), nullable=False)

class SoftDeleteMixin:
    deleted_at: Mapped[Optional[datetime]] = mapped_column(DateTime(timezone=True), nullable=True, default=None)
```

From backend/app/models/consultation.py (REFERENCE PATTERN):
```python
class Consultation(TimestampMixin, SoftDeleteMixin, Base):
    __tablename__ = "consultations"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    patient_id: Mapped[int] = mapped_column(Integer, ForeignKey("patients.id"), nullable=False)
    recorded_by: Mapped[int] = mapped_column(Integer, ForeignKey("users.id"), nullable=False)
    # ... columns ...
    patient = relationship("Patient", back_populates="consultations", lazy="raise")
    recorded_by_user = relationship("User", lazy="raise")
    __table_args__ = (
        Index("ix_consultations_patient_id", "patient_id"),
    )
```

From backend/app/models/__init__.py:
```python
from app.models.barangay import Barangay
from app.models.health_station import HealthStation
from app.models.user import User
from app.models.user_session import UserSession
from app.models.patient import Patient
from app.models.consultation import Consultation
__all__ = ["Barangay", "HealthStation", "User", "UserSession", "Patient", "Consultation"]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create prenatal and postpartum ORM models</name>
  <files>backend/app/models/prenatal.py, backend/app/models/postpartum.py</files>
  <read_first>
    - backend/app/models/consultation.py (reference pattern for FKs, lazy="raise", __table_args__)
    - backend/app/core/base.py (TimestampMixin, SoftDeleteMixin imports)
    - .planning/phases/04.1-mch-shared-data-model/04.1-CONTEXT.md (locked decisions)
  </read_first>
  <action>
Create `backend/app/models/prenatal.py` with two classes:

**PrenatalEnrollment:**
- `__tablename__ = "prenatal_enrollments"`
- MRO: `class PrenatalEnrollment(TimestampMixin, SoftDeleteMixin, Base):`
- Columns:
  - `id: Mapped[int]` — Integer, primary_key=True, autoincrement=True
  - `patient_id: Mapped[int]` — Integer, ForeignKey("patients.id"), nullable=False
  - `health_station_id: Mapped[int]` — Integer, ForeignKey("health_stations.id"), nullable=False
  - `recorded_by: Mapped[int]` — Integer, ForeignKey("users.id"), nullable=False
  - `gravida: Mapped[int]` — Integer, nullable=False
  - `para: Mapped[int]` — Integer, nullable=False
  - `lmp: Mapped[date]` — Date, nullable=False (last menstrual period)
  - `edc: Mapped[date]` — Date, nullable=False (estimated date of confinement)
  - `risk_factors: Mapped[str | None]` — Text, nullable=True (free-text nurse notes)
  - `is_high_risk: Mapped[bool]` — Boolean, nullable=False, default=False, server_default="false" (ML Phase 8 flag)
  - `next_visit_date: Mapped[date | None]` — Date, nullable=True (updated by service layer after each visit)
  - `local_id: Mapped[uuid.UUID | None]` — postgresql.UUID(as_uuid=True), nullable=True (offline sync)
  - `status: Mapped[str]` — Text, nullable=False, server_default="PENDING" (record_status — will be mapped to shared ENUM in migration)
- Relationships: `patient = relationship("Patient", lazy="raise")`, `health_station = relationship("HealthStation", lazy="raise")`, `recorded_by_user = relationship("User", lazy="raise")`, `visits = relationship("PrenatalVisit", back_populates="enrollment", lazy="raise")`
- `__table_args__`: Index on patient_id, Index on health_station_id

**PrenatalVisit:**
- `__tablename__ = "prenatal_visits"`
- MRO: `class PrenatalVisit(TimestampMixin, SoftDeleteMixin, Base):`
- Columns:
  - `id: Mapped[int]` — Integer, primary_key=True, autoincrement=True
  - `prenatal_enrollment_id: Mapped[int]` — Integer, ForeignKey("prenatal_enrollments.id"), nullable=False
  - `recorded_by: Mapped[int]` — Integer, ForeignKey("users.id"), nullable=False
  - `visit_date: Mapped[date]` — Date, nullable=False
  - `aog_weeks: Mapped[int | None]` — Integer, nullable=True (age of gestation in weeks)
  - `weight: Mapped[Decimal | None]` — Numeric(5,2), nullable=True
  - `bp_systolic: Mapped[int | None]` — Integer, nullable=True
  - `bp_diastolic: Mapped[int | None]` — Integer, nullable=True
  - `fundic_height: Mapped[Decimal | None]` — Numeric(4,1), nullable=True
  - `fetal_heart_tone: Mapped[int | None]` — Integer, nullable=True (FHT in bpm)
  - `presentation: Mapped[str | None]` — Text, nullable=True (cephalic, breech, etc.)
  - `edema: Mapped[str | None]` — Text, nullable=True
  - `hgb: Mapped[Decimal | None]` — Numeric(4,1), nullable=True (hemoglobin g/dL)
  - `gdm_positive: Mapped[bool | None]` — Boolean, nullable=True (gestational diabetes)
  - `tt_dose: Mapped[int | None]` — Integer, nullable=True (tetanus toxoid dose number)
  - `iron_supplementation: Mapped[bool | None]` — Boolean, nullable=True
  - `counseling_notes: Mapped[str | None]` — Text, nullable=True
  - `local_id: Mapped[uuid.UUID | None]` — postgresql.UUID(as_uuid=True), nullable=True
  - `status: Mapped[str]` — Text, nullable=False, server_default="PENDING"
- NO `health_station_id` column (isolation via JOIN through enrollment per CONTEXT.md decision)
- Relationships: `enrollment = relationship("PrenatalEnrollment", back_populates="visits", lazy="raise")`, `recorded_by_user = relationship("User", lazy="raise")`
- `__table_args__`: Index on prenatal_enrollment_id

Create `backend/app/models/postpartum.py` with two classes:

**PostpartumEnrollment:**
- `__tablename__ = "postpartum_enrollments"`
- MRO: `class PostpartumEnrollment(TimestampMixin, SoftDeleteMixin, Base):`
- Columns:
  - `id: Mapped[int]` — Integer, primary_key=True, autoincrement=True
  - `patient_id: Mapped[int]` — Integer, ForeignKey("patients.id"), nullable=False
  - `health_station_id: Mapped[int]` — Integer, ForeignKey("health_stations.id"), nullable=False
  - `recorded_by: Mapped[int]` — Integer, ForeignKey("users.id"), nullable=False
  - `prenatal_enrollment_id: Mapped[int | None]` — Integer, ForeignKey("prenatal_enrollments.id", ondelete="SET NULL"), nullable=True (nullable per CONTEXT.md — external facility deliveries)
  - `delivery_date: Mapped[date]` — Date, nullable=False
  - `delivery_type: Mapped[str | None]` — Text, nullable=True (NSD, CS, etc.)
  - `birth_outcome: Mapped[str | None]` — Text, nullable=True (live birth, stillbirth, etc.)
  - `day1_date: Mapped[date | None]` — Date, nullable=True (computed from delivery_date by service)
  - `week1_date: Mapped[date | None]` — Date, nullable=True
  - `week6_date: Mapped[date | None]` — Date, nullable=True
  - `local_id: Mapped[uuid.UUID | None]` — postgresql.UUID(as_uuid=True), nullable=True
  - `status: Mapped[str]` — Text, nullable=False, server_default="PENDING"
- Relationships: `patient = relationship("Patient", lazy="raise")`, `health_station = relationship("HealthStation", lazy="raise")`, `recorded_by_user = relationship("User", lazy="raise")`, `prenatal_enrollment = relationship("PrenatalEnrollment", lazy="raise")`, `visits = relationship("PostpartumVisit", back_populates="enrollment", lazy="raise")`
- `__table_args__`: Index on patient_id, Index on health_station_id

**PostpartumVisit:**
- `__tablename__ = "postpartum_visits"`
- MRO: `class PostpartumVisit(TimestampMixin, SoftDeleteMixin, Base):`
- Columns:
  - `id: Mapped[int]` — Integer, primary_key=True, autoincrement=True
  - `postpartum_enrollment_id: Mapped[int]` — Integer, ForeignKey("postpartum_enrollments.id"), nullable=False
  - `recorded_by: Mapped[int]` — Integer, ForeignKey("users.id"), nullable=False
  - `visit_date: Mapped[date]` — Date, nullable=False
  - `visit_type: Mapped[str]` — Text, nullable=False (one of: "day1", "week1", "week6")
  - `bp_systolic: Mapped[int | None]` — Integer, nullable=True
  - `bp_diastolic: Mapped[int | None]` — Integer, nullable=True
  - `wound_status: Mapped[str | None]` — Text, nullable=True (uterine involution / wound)
  - `breastfeeding: Mapped[bool | None]` — Boolean, nullable=True
  - `family_planning_counseling: Mapped[bool | None]` — Boolean, nullable=True
  - `newborn_status: Mapped[str | None]` — Text, nullable=True
  - `notes: Mapped[str | None]` — Text, nullable=True
  - `local_id: Mapped[uuid.UUID | None]` — postgresql.UUID(as_uuid=True), nullable=True
  - `status: Mapped[str]` — Text, nullable=False, server_default="PENDING"
- NO `health_station_id` column
- Relationships: `enrollment = relationship("PostpartumEnrollment", back_populates="visits", lazy="raise")`, `recorded_by_user = relationship("User", lazy="raise")`
- `__table_args__`: Index on postpartum_enrollment_id

Import pattern for both files — follow consultation.py exactly:
```python
from datetime import date
from decimal import Decimal
import uuid
from sqlalchemy import Text, Integer, Date, Boolean, ForeignKey, Numeric, Index
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.orm import Mapped, mapped_column, relationship
from app.core.base import Base, TimestampMixin, SoftDeleteMixin
```
  </action>
  <verify>
    <automated>cd D:/capstone-hsms && python -c "from app.models.prenatal import PrenatalEnrollment, PrenatalVisit; from app.models.postpartum import PostpartumEnrollment, PostpartumVisit; print('OK')" 2>&1 || docker-compose exec -T backend python -c "from app.models.prenatal import PrenatalEnrollment, PrenatalVisit; from app.models.postpartum import PostpartumEnrollment, PostpartumVisit; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/models/prenatal.py contains `class PrenatalEnrollment(TimestampMixin, SoftDeleteMixin, Base):`
    - backend/app/models/prenatal.py contains `class PrenatalVisit(TimestampMixin, SoftDeleteMixin, Base):`
    - backend/app/models/prenatal.py contains `is_high_risk: Mapped[bool]`
    - backend/app/models/prenatal.py contains `next_visit_date: Mapped[date | None]`
    - backend/app/models/prenatal.py contains `local_id: Mapped[uuid.UUID | None]`
    - backend/app/models/prenatal.py contains `ForeignKey("patients.id")`
    - backend/app/models/prenatal.py contains `ForeignKey("health_stations.id")`
    - backend/app/models/prenatal.py contains `ForeignKey("users.id")`
    - backend/app/models/prenatal.py contains `lazy="raise"` on every relationship
    - PrenatalVisit does NOT contain `health_station_id`
    - backend/app/models/postpartum.py contains `class PostpartumEnrollment(TimestampMixin, SoftDeleteMixin, Base):`
    - backend/app/models/postpartum.py contains `class PostpartumVisit(TimestampMixin, SoftDeleteMixin, Base):`
    - backend/app/models/postpartum.py contains `ForeignKey("prenatal_enrollments.id", ondelete="SET NULL")`
    - backend/app/models/postpartum.py contains `day1_date`, `week1_date`, `week6_date`
    - backend/app/models/postpartum.py contains `prenatal_enrollment_id: Mapped[int | None]` (nullable)
    - PostpartumVisit does NOT contain `health_station_id`
  </acceptance_criteria>
  <done>PrenatalEnrollment, PrenatalVisit, PostpartumEnrollment, PostpartumVisit models importable with correct columns, FKs, mixins, lazy="raise", and offline sync fields</done>
</task>

<task type="auto">
  <name>Task 2: Create EPI and nutrition ORM models + update __init__.py</name>
  <files>backend/app/models/epi.py, backend/app/models/nutrition.py, backend/app/models/__init__.py</files>
  <read_first>
    - backend/app/models/consultation.py (reference pattern)
    - backend/app/models/__init__.py (current exports to extend)
    - .planning/phases/04.1-mch-shared-data-model/04.1-CONTEXT.md (locked decisions)
  </read_first>
  <action>
Create `backend/app/models/epi.py` with two classes:

**EpiEnrollment:**
- `__tablename__ = "epi_enrollments"`
- MRO: `class EpiEnrollment(TimestampMixin, SoftDeleteMixin, Base):`
- Columns:
  - `id: Mapped[int]` — Integer, primary_key=True, autoincrement=True
  - `patient_id: Mapped[int]` — Integer, ForeignKey("patients.id"), nullable=False
  - `health_station_id: Mapped[int]` — Integer, ForeignKey("health_stations.id"), nullable=False
  - `recorded_by: Mapped[int]` — Integer, ForeignKey("users.id"), nullable=False
  - `enrollment_date: Mapped[date]` — Date, nullable=False
  - `fic_status: Mapped[bool]` — Boolean, nullable=False, default=False, server_default="false" (ML Phase 8 barangay risk index flag)
  - `next_visit_date: Mapped[date | None]` — Date, nullable=True (earliest upcoming dose across all vaccine types; recomputed by service after each vaccination)
  - `local_id: Mapped[uuid.UUID | None]` — postgresql.UUID(as_uuid=True), nullable=True
  - `status: Mapped[str]` — Text, nullable=False, server_default="PENDING"
- Relationships: `patient = relationship("Patient", lazy="raise")`, `health_station = relationship("HealthStation", lazy="raise")`, `recorded_by_user = relationship("User", lazy="raise")`, `vaccinations = relationship("EpiVaccination", back_populates="enrollment", lazy="raise")`
- `__table_args__`: Index on patient_id, Index on health_station_id

**EpiVaccination:**
- `__tablename__ = "epi_vaccinations"`
- MRO: `class EpiVaccination(TimestampMixin, SoftDeleteMixin, Base):`
- Columns:
  - `id: Mapped[int]` — Integer, primary_key=True, autoincrement=True
  - `epi_enrollment_id: Mapped[int]` — Integer, ForeignKey("epi_enrollments.id"), nullable=False
  - `recorded_by: Mapped[int]` — Integer, ForeignKey("users.id"), nullable=False
  - `vaccine: Mapped[str]` — Text, nullable=False (NOT PostgreSQL ENUM per CONTEXT.md — validated at Pydantic layer)
  - `dose_number: Mapped[int]` — Integer, nullable=False
  - `date_given: Mapped[date]` — Date, nullable=False
  - `lot_number: Mapped[str | None]` — Text, nullable=True
  - `site: Mapped[str | None]` — Text, nullable=True (injection site: left deltoid, right thigh, etc.)
  - `administered_by: Mapped[str | None]` — Text, nullable=True (name of person who administered)
  - `local_id: Mapped[uuid.UUID | None]` — postgresql.UUID(as_uuid=True), nullable=True
  - `status: Mapped[str]` — Text, nullable=False, server_default="PENDING"
- NO `health_station_id` column (isolation via JOIN through enrollment)
- Relationships: `enrollment = relationship("EpiEnrollment", back_populates="vaccinations", lazy="raise")`, `recorded_by_user = relationship("User", lazy="raise")`
- `__table_args__`: Index on epi_enrollment_id, unique constraint on (epi_enrollment_id, vaccine, dose_number) to prevent duplicate dose recording

Create `backend/app/models/nutrition.py` with two classes:

**NutritionEnrollment:**
- `__tablename__ = "nutrition_enrollments"`
- MRO: `class NutritionEnrollment(TimestampMixin, SoftDeleteMixin, Base):`
- Columns:
  - `id: Mapped[int]` — Integer, primary_key=True, autoincrement=True
  - `patient_id: Mapped[int]` — Integer, ForeignKey("patients.id"), nullable=False
  - `health_station_id: Mapped[int]` — Integer, ForeignKey("health_stations.id"), nullable=False
  - `recorded_by: Mapped[int]` — Integer, ForeignKey("users.id"), nullable=False
  - `enrollment_date: Mapped[date]` — Date, nullable=False
  - `local_id: Mapped[uuid.UUID | None]` — postgresql.UUID(as_uuid=True), nullable=True
  - `status: Mapped[str]` — Text, nullable=False, server_default="PENDING"
- Relationships: `patient = relationship("Patient", lazy="raise")`, `health_station = relationship("HealthStation", lazy="raise")`, `recorded_by_user = relationship("User", lazy="raise")`, `visits = relationship("NutritionVisit", back_populates="enrollment", lazy="raise")`
- `__table_args__`: Index on patient_id, Index on health_station_id

**NutritionVisit:**
- `__tablename__ = "nutrition_visits"`
- MRO: `class NutritionVisit(TimestampMixin, SoftDeleteMixin, Base):`
- Columns:
  - `id: Mapped[int]` — Integer, primary_key=True, autoincrement=True
  - `nutrition_enrollment_id: Mapped[int]` — Integer, ForeignKey("nutrition_enrollments.id"), nullable=False
  - `recorded_by: Mapped[int]` — Integer, ForeignKey("users.id"), nullable=False
  - `visit_date: Mapped[date]` — Date, nullable=False
  - `weight: Mapped[Decimal]` — Numeric(5,2), nullable=False (kg)
  - `height: Mapped[Decimal]` — Numeric(5,2), nullable=False (cm)
  - `muac: Mapped[Decimal | None]` — Numeric(4,1), nullable=True (mid-upper arm circumference in cm)
  - `waz: Mapped[Decimal | None]` — Numeric(5,2), nullable=True (weight-for-age Z-score, computed by service)
  - `haz: Mapped[Decimal | None]` — Numeric(5,2), nullable=True (height-for-age Z-score)
  - `whz: Mapped[Decimal | None]` — Numeric(5,2), nullable=True (weight-for-height Z-score)
  - `nutrition_status: Mapped[str | None]` — Text, nullable=True (NORMAL, UNDERWEIGHT, STUNTED, WASTED, SEVERELY_WASTED — set by service layer)
  - `severe_wasting: Mapped[bool]` — Boolean, nullable=False, default=False, server_default="false" (ML Phase 8 flag, set when WHZ < -3)
  - `local_id: Mapped[uuid.UUID | None]` — postgresql.UUID(as_uuid=True), nullable=True
  - `status: Mapped[str]` — Text, nullable=False, server_default="PENDING"
- NO `health_station_id` column
- Relationships: `enrollment = relationship("NutritionEnrollment", back_populates="visits", lazy="raise")`, `recorded_by_user = relationship("User", lazy="raise")`
- `__table_args__`: Index on nutrition_enrollment_id

**Update `backend/app/models/__init__.py`** — add all 8 new model imports and extend `__all__`:
```python
from app.models.prenatal import PrenatalEnrollment, PrenatalVisit  # noqa: F401
from app.models.postpartum import PostpartumEnrollment, PostpartumVisit  # noqa: F401
from app.models.epi import EpiEnrollment, EpiVaccination  # noqa: F401
from app.models.nutrition import NutritionEnrollment, NutritionVisit  # noqa: F401

__all__ = [
    "Barangay", "HealthStation", "User", "UserSession", "Patient", "Consultation",
    "PrenatalEnrollment", "PrenatalVisit", "PostpartumEnrollment", "PostpartumVisit",
    "EpiEnrollment", "EpiVaccination", "NutritionEnrollment", "NutritionVisit",
]
```
  </action>
  <verify>
    <automated>cd D:/capstone-hsms && python -c "from app.models import PrenatalEnrollment, PrenatalVisit, PostpartumEnrollment, PostpartumVisit, EpiEnrollment, EpiVaccination, NutritionEnrollment, NutritionVisit; print('All 8 models imported OK')" 2>&1 || docker-compose exec -T backend python -c "from app.models import PrenatalEnrollment, PrenatalVisit, PostpartumEnrollment, PostpartumVisit, EpiEnrollment, EpiVaccination, NutritionEnrollment, NutritionVisit; print('All 8 models imported OK')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/models/epi.py contains `class EpiEnrollment(TimestampMixin, SoftDeleteMixin, Base):`
    - backend/app/models/epi.py contains `class EpiVaccination(TimestampMixin, SoftDeleteMixin, Base):`
    - backend/app/models/epi.py contains `fic_status: Mapped[bool]`
    - backend/app/models/epi.py contains `vaccine: Mapped[str]` with Text type (NOT Enum)
    - backend/app/models/epi.py contains `next_visit_date: Mapped[date | None]`
    - EpiVaccination does NOT contain `health_station_id`
    - backend/app/models/nutrition.py contains `class NutritionEnrollment(TimestampMixin, SoftDeleteMixin, Base):`
    - backend/app/models/nutrition.py contains `class NutritionVisit(TimestampMixin, SoftDeleteMixin, Base):`
    - backend/app/models/nutrition.py contains `severe_wasting: Mapped[bool]`
    - backend/app/models/nutrition.py contains `waz: Mapped[Decimal | None]`
    - backend/app/models/nutrition.py contains `haz: Mapped[Decimal | None]`
    - backend/app/models/nutrition.py contains `whz: Mapped[Decimal | None]`
    - backend/app/models/nutrition.py contains `nutrition_status: Mapped[str | None]`
    - NutritionVisit does NOT contain `health_station_id`
    - backend/app/models/__init__.py contains `PrenatalEnrollment`
    - backend/app/models/__init__.py contains `NutritionVisit`
    - backend/app/models/__init__.py __all__ list has 14 entries
  </acceptance_criteria>
  <done>All 8 MCH ORM models importable from backend/app/models, with correct ML fields (is_high_risk, fic_status, severe_wasting), offline sync fields (local_id, status), and proper FK relationships</done>
</task>

</tasks>

<verification>
- All 8 models import successfully from `backend/app/models`
- Each enrollment model has patient_id, health_station_id, recorded_by FKs
- Each visit model has enrollment FK and recorded_by FK but NOT health_station_id
- ML fields present: `is_high_risk` (prenatal), `fic_status` (epi), `severe_wasting` (nutrition)
- All relationships use `lazy="raise"`
- All 8 models carry `local_id` and `status` columns
</verification>

<success_criteria>
All 8 MCH ORM model classes exist, import cleanly from `backend/app/models`, and have the exact column names and FK relationships specified in the CONTEXT.md decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-mch-shared-data-model/04.1-01-SUMMARY.md`
</output>
