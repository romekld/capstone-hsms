---
phase: 02-authentication-rbac-user-management
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_auth/__init__.py
  - backend/tests/test_auth/test_login.py
  - backend/tests/test_auth/test_logout.py
  - backend/tests/test_auth/test_refresh.py
  - backend/tests/test_auth/test_rbac.py
  - backend/tests/test_auth/test_auth_guard.py
  - backend/tests/test_auth/test_base_repository.py
  - backend/tests/test_admin/__init__.py
  - backend/tests/test_admin/test_user_create.py
  - backend/pytest.ini
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07
  - AUTH-08
  - AUTH-09
  - AUTH-10

must_haves:
  truths:
    - "pytest --collect-only exits 0 and lists all auth and admin test functions"
    - "All test stubs are marked xfail or skip pending implementation — they do not cause false positives"
    - "test_rbac.py covers test_role_guard, test_dual_role, test_dso_write_blocked"
    - "test_base_repository.py covers test_isolation_filter, test_cross_bhs_bypass, test_cho_cross_bhs"
    - "test_user_create.py covers test_admin_exclusive (AUTH-04) and test_non_admin_blocked (AUTH-06)"
  artifacts:
    - path: "backend/tests/test_auth/__init__.py"
      provides: "test_auth package init"
    - path: "backend/tests/test_auth/test_login.py"
      provides: "AUTH-01 login flow stubs"
      contains: "test_login_success"
    - path: "backend/tests/test_auth/test_rbac.py"
      provides: "AUTH-03, AUTH-05, AUTH-10 role guard stubs"
      contains: "test_role_guard"
    - path: "backend/tests/test_auth/test_base_repository.py"
      provides: "AUTH-08, AUTH-09 isolation filter stubs"
      contains: "test_isolation_filter"
    - path: "backend/tests/test_admin/test_user_create.py"
      provides: "AUTH-04, AUTH-06 admin stubs"
      contains: "test_admin_exclusive"
  key_links:
    - from: "backend/tests/conftest.py"
      to: "backend/tests/test_auth/"
      via: "async_session fixture inherited"
      pattern: "async_session"
---

<objective>
Create the full Wave 0 test scaffold per the VALIDATION.md spec — all test packages, test files with stub functions, and pytest.ini verification. These stubs define the test names that implementation plans must satisfy.

Purpose: Nyquist rule requires test files to exist before implementation tasks run. All 15 test cases from VALIDATION.md need stub functions so `<automated>` verify commands in later plans can reference them.
Output: 9 test files (2 __init__.py, 7 test modules), all stubs defined, pytest collects cleanly.
</objective>

<execution_context>
@C:/Users/jerom/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jerom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-authentication-rbac-user-management/02-VALIDATION.md
@.planning/phases/02-authentication-rbac-user-management/02-RESEARCH.md

<interfaces>
From backend/tests/conftest.py:
```python
# async_session fixture available to all tests via inheritance
@pytest_asyncio.fixture
async def async_session(async_engine) -> AsyncSession:
    async with async_sessionmaker(async_engine, expire_on_commit=False)() as session:
        yield session
```

From backend/pytest.ini (already exists per Plan 01):
```ini
# Check for asyncio_mode = auto — add if missing
```

Test function naming (from VALIDATION.md):
- test_login.py: test_login_success, test_wrong_password, test_inactive_user
- test_logout.py: test_logout_revokes_session
- test_refresh.py: test_refresh_rotates_token, test_revoked_token
- test_rbac.py: test_role_guard, test_dual_role, test_dso_write_blocked
- test_auth_guard.py: test_unauthenticated_returns_401
- test_base_repository.py: test_isolation_filter, test_cross_bhs_bypass, test_cho_cross_bhs
- test_admin/test_user_create.py: test_create_user_success, test_admin_exclusive, test_non_admin_blocked
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify pytest.ini and create test package __init__.py files</name>
  <read_first>
    - backend/pytest.ini (check asyncio_mode = auto is set; check testpaths)
    - backend/tests/__init__.py (exists from Phase 1)
    - backend/tests/test_infra/__init__.py (exists from Phase 1 — pattern reference)
  </read_first>
  <action>
1. Read backend/pytest.ini. If it contains `asyncio_mode = auto`, no change needed. If not, add it under `[pytest]`:
   ```ini
   [pytest]
   asyncio_mode = auto
   testpaths = tests
   ```

2. Create backend/tests/test_auth/__init__.py (empty file — just the package marker):
   ```python
   ```

3. Create backend/tests/test_admin/__init__.py (empty file):
   ```python
   ```
  </action>
  <verify>
    <automated>docker-compose exec backend pytest tests/test_auth/ tests/test_admin/ --collect-only -q 2>&1 | grep "error" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_auth/__init__.py exists (can be empty)
    - backend/tests/test_admin/__init__.py exists (can be empty)
    - backend/pytest.ini contains "asyncio_mode = auto"
    - docker-compose exec backend pytest --collect-only -q exits 0 (no collection errors)
  </acceptance_criteria>
  <done>Test packages exist; pytest collects without import errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for auth endpoint tests (login, logout, refresh, auth_guard)</name>
  <read_first>
    - backend/tests/test_infra/test_health.py (stub pattern — how existing stubs use pytest.mark.asyncio and httpx)
    - backend/tests/conftest.py (available fixtures)
    - backend/app/main.py (app import pattern used in tests)
  </read_first>
  <action>
1. Create backend/tests/test_auth/test_login.py:
   ```python
   """AUTH-01: Login flow — email/password → access+refresh token pair.
   Stubs: pass until Wave 2 (Plan 02-03) ships POST /auth/login.
   """
   import pytest
   from httpx import ASGITransport, AsyncClient
   from app.main import app


   @pytest.mark.asyncio
   async def test_login_success():
       """POST /auth/login with valid credentials returns 200 + access_token + refresh_token."""
       pytest.skip("Stub — requires Plan 02-03 POST /auth/login endpoint")


   @pytest.mark.asyncio
   async def test_wrong_password():
       """POST /auth/login with wrong password returns 401."""
       pytest.skip("Stub — requires Plan 02-03 POST /auth/login endpoint")


   @pytest.mark.asyncio
   async def test_inactive_user():
       """POST /auth/login with inactive user (is_active=False) returns 401."""
       pytest.skip("Stub — requires Plan 02-03 POST /auth/login endpoint")
   ```

2. Create backend/tests/test_auth/test_logout.py:
   ```python
   """AUTH-02: Logout — revokes user_sessions row server-side.
   Stubs: pass until Wave 2 (Plan 02-03) ships POST /auth/logout.
   """
   import pytest


   @pytest.mark.asyncio
   async def test_logout_revokes_session():
       """POST /auth/logout sets revoked_at on the user_sessions row; subsequent refresh fails."""
       pytest.skip("Stub — requires Plan 02-03 POST /auth/logout endpoint")
   ```

3. Create backend/tests/test_auth/test_refresh.py:
   ```python
   """AUTH-02: Refresh token rotation — old token invalidated, new pair issued.
   Stubs: pass until Wave 2 (Plan 02-03) ships POST /auth/refresh.
   """
   import pytest


   @pytest.mark.asyncio
   async def test_refresh_rotates_token():
       """POST /auth/refresh issues new token pair and revokes the old refresh token."""
       pytest.skip("Stub — requires Plan 02-03 POST /auth/refresh endpoint")


   @pytest.mark.asyncio
   async def test_revoked_token():
       """POST /auth/refresh with an already-revoked token returns 401."""
       pytest.skip("Stub — requires Plan 02-03 POST /auth/refresh endpoint")
   ```

4. Create backend/tests/test_auth/test_auth_guard.py:
   ```python
   """AUTH-07: Unauthenticated requests to protected endpoints return 401.
   Stubs: pass until Wave 2 (Plan 02-03) ships require_role() dependency.
   """
   import pytest


   @pytest.mark.asyncio
   async def test_unauthenticated_returns_401():
       """GET /admin/users without Authorization header returns 401."""
       pytest.skip("Stub — requires Plan 02-03 require_role() dependency and Plan 02-04 admin router")
   ```
  </action>
  <verify>
    <automated>docker-compose exec backend pytest tests/test_auth/test_login.py tests/test_auth/test_logout.py tests/test_auth/test_refresh.py tests/test_auth/test_auth_guard.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_auth/test_login.py contains "def test_login_success"
    - backend/tests/test_auth/test_login.py contains "def test_wrong_password"
    - backend/tests/test_auth/test_login.py contains "def test_inactive_user"
    - backend/tests/test_auth/test_logout.py contains "def test_logout_revokes_session"
    - backend/tests/test_auth/test_refresh.py contains "def test_refresh_rotates_token"
    - backend/tests/test_auth/test_refresh.py contains "def test_revoked_token"
    - backend/tests/test_auth/test_auth_guard.py contains "def test_unauthenticated_returns_401"
    - docker-compose exec backend pytest tests/test_auth/ --collect-only -q exits 0 with no errors
    - docker-compose exec backend pytest tests/test_auth/ -x -q exits 0 (all stubs skip, none fail)
  </acceptance_criteria>
  <done>4 auth test files collect and run (all skip); test names match VALIDATION.md.</done>
</task>

<task type="auto">
  <name>Task 3: Create test stubs for RBAC, BaseRepository, and admin user management</name>
  <read_first>
    - backend/tests/test_auth/test_login.py (pattern established in Task 2 — follow same skip pattern)
    - backend/tests/conftest.py (async_session fixture)
  </read_first>
  <action>
1. Create backend/tests/test_auth/test_rbac.py:
   ```python
   """AUTH-03/05/10: RBAC role guards — require_role() enforcement.
   Stubs: pass until Wave 2 (Plan 02-03) ships require_role() and CurrentUser dependencies.
   """
   import pytest


   @pytest.mark.asyncio
   async def test_role_guard():
       """require_role(["nurse"]) blocks a BHW user with HTTP 403."""
       pytest.skip("Stub — requires Plan 02-03 require_role() dependency")


   @pytest.mark.asyncio
   async def test_dual_role():
       """User with roles=['nurse','disease_surveillance_officer'] passes both nurse and DSO route guards."""
       pytest.skip("Stub — requires Plan 02-03 require_role() dependency")


   @pytest.mark.asyncio
   async def test_dso_write_blocked():
       """DSO is NOT in allowed roles for non-PIDSR write endpoints; gets 403 on POST /patients (Phase 3)."""
       pytest.skip("Stub — requires Plan 02-03 require_role() and Plan 02-04 admin router")
   ```

2. Create backend/tests/test_auth/test_base_repository.py:
   ```python
   """AUTH-08/09: BaseRepository barangay isolation filter.
   Stubs: pass until Wave 2 (Plan 02-03) ships BaseRepository.
   """
   import pytest


   def test_isolation_filter():
       """BaseRepository._isolation_filter() adds WHERE health_station_id = user.health_station_id for nurse."""
       pytest.skip("Stub — requires Plan 02-03 BaseRepository class")


   def test_cross_bhs_bypass():
       """BaseRepository._isolation_filter() skips WHERE clause for city_health_officer."""
       pytest.skip("Stub — requires Plan 02-03 BaseRepository class")


   def test_cho_cross_bhs():
       """CHO user (health_station_id=None) in CROSS_BHS_ROLES sees all BHS data — no isolation filter applied."""
       pytest.skip("Stub — requires Plan 02-03 BaseRepository class")
   ```

3. Create backend/tests/test_admin/test_user_create.py:
   ```python
   """AUTH-04/06: Admin user management — create user, system_admin exclusivity, non-admin access block.
   Stubs: pass until Wave 2 (Plan 02-04) ships POST /admin/users.
   """
   import pytest
   from httpx import ASGITransport, AsyncClient
   from app.main import app


   @pytest.mark.asyncio
   async def test_create_user_success():
       """POST /admin/users by system_admin creates a user and returns 201 with user object."""
       pytest.skip("Stub — requires Plan 02-04 POST /admin/users endpoint")


   @pytest.mark.asyncio
   async def test_admin_exclusive():
       """POST /admin/users with roles=['system_admin','nurse'] returns 422 with field-level error."""
       pytest.skip("Stub — requires Plan 02-04 POST /admin/users + service validation")


   @pytest.mark.asyncio
   async def test_non_admin_blocked():
       """POST /admin/users by a nurse returns 403."""
       pytest.skip("Stub — requires Plan 02-04 POST /admin/users + require_role(['system_admin'])")
   ```
  </action>
  <verify>
    <automated>docker-compose exec backend pytest tests/test_auth/test_rbac.py tests/test_auth/test_base_repository.py tests/test_admin/test_user_create.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_auth/test_rbac.py contains "def test_role_guard"
    - backend/tests/test_auth/test_rbac.py contains "def test_dual_role"
    - backend/tests/test_auth/test_rbac.py contains "def test_dso_write_blocked"
    - backend/tests/test_auth/test_base_repository.py contains "def test_isolation_filter"
    - backend/tests/test_auth/test_base_repository.py contains "def test_cross_bhs_bypass"
    - backend/tests/test_auth/test_base_repository.py contains "def test_cho_cross_bhs"
    - backend/tests/test_admin/test_user_create.py contains "def test_create_user_success"
    - backend/tests/test_admin/test_user_create.py contains "def test_admin_exclusive"
    - backend/tests/test_admin/test_user_create.py contains "def test_non_admin_blocked"
    - docker-compose exec backend pytest tests/test_auth/ tests/test_admin/ -x -q exits 0 (all skip, none fail)
    - docker-compose exec backend pytest tests/test_auth/ tests/test_admin/ --collect-only -q output contains "12 tests collected" or more
  </acceptance_criteria>
  <done>All 12 stub functions defined; full test suite collects and skips cleanly; no false failures.</done>
</task>

</tasks>

<verification>
Full suite collection check:
```bash
docker-compose exec backend pytest --collect-only -q 2>&1 | tail -5
# Expected: X tests collected, 0 errors
docker-compose exec backend pytest tests/test_auth/ tests/test_admin/ -x -q
# Expected: all skip, 0 failed
```
</verification>

<success_criteria>
- 2 test package __init__.py files exist (test_auth, test_admin)
- 7 test module files exist with exact function names matching VALIDATION.md
- pytest.ini has asyncio_mode = auto
- `docker-compose exec backend pytest --collect-only -q` exits 0 with no collection errors
- `docker-compose exec backend pytest tests/test_auth/ tests/test_admin/ -x -q` exits 0 (all skip)
- All 12 stub functions use pytest.skip() with descriptive plan reference messages
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-rbac-user-management/02-02-SUMMARY.md`
</output>
